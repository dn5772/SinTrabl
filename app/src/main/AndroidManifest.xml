<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.sintrabel">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SinTrabel"
        android:usesCleartextTraffic="true"
        tools:targetApi="m">
        <activity android:name=".activitiy.testActivity"></activity>
        <activity
            android:name=".activitiy.SettingsActivity"
            android:label="@string/title_activity_settings" />
        <activity android:name=".activitiy.BadgeActivity" />
        <activity android:name=".activitiy.SignupActivity" />
        <activity
            android:name=".activitiy.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activitiy.BookmarkFolderActivity" />
        <activity android:name=".activitiy.PlaceActivity" />
        <activity
            android:name=".activitiy.BookmarkActivity"
            android:exported="true" />
        <activity
            android:name=".activitiy.SearchActivity"
            android:exported="true" />
        <activity
            android:name=".activitiy.MainActivity"
            android:exported="true"></activity>
    </application>

</manifest>